# Normal mode profile — sourced by distro-modectl.
MODE_NAME="Normal"
CPU_GOVERNOR="schedutil"
POWER_PROFILE="balanced"
ENABLE_SERVICES=()
DISABLE_SERVICES=()
STOP_AI_PRESET=true
DE_DCONF_FILE=""
CONFIRM_DISPLAY_MANAGER_STOP=false
NOTES="macOS-style shell (dock, top bar) is NOT implemented by this script — that's a GNOME extension install + dconf config job, not a power/service toggle, and needs a real desktop session to build/test against. See modes/modectl/README.md TODOs."
