[Desktop Entry]
Type=Application
# NOT "Software Update". That was tried for one commit and immediately reported
# as a duplicate: Ubuntu's update-manager is called "Software Updater", and two
# near-identically named entries in the grid is worse than one badly-named one —
# you now have to guess which is which, every time. The name says whose updates
# these are; the KEYWORDS below are what makes it findable, which is the job the
# rename was actually trying to do.
Name=Refract Updates
GenericName=Update Refract OS
Comment=Install updates to Refract OS — modes, layouts, the top-bar switcher and the distro-* tools
Exec=/usr/local/bin/refract-updates
Icon=software-update-available
Terminal=false
Categories=System;Settings;PackageManager;
# Every word someone might plausibly type into Activities to find this. The old
# list was update;upgrade;refract;system; — which misses "software update", the
# single most likely search, and every phrasing a Windows or macOS user brings
# with them.
Keywords=update;updates;upgrade;software update;system update;refract;patch;version;install updates;check for updates;
StartupNotify=true
